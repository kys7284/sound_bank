<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.boot.sound.exchange.AdminExchangeDAO">

  <!-- 외환 계좌 개설 요청 리스트 조회 -->
  <select id="reqList" resultType="com.boot.sound.exchange.ExchangeAccountRequestDTO">
    SELECT 
      REQUEST_ID,
      CUSTOMER_ID,
      WITHDRAW_ACCOUNT_NUMBER,
      CURRENCY_TYPE,
      EXCHANGE_ACCOUNT_PWD,
      STATUS,
      REQUEST_DATE
    FROM exchange_account_request
    ORDER BY REQUEST_DATE DESC
  </select>

  <!-- 외환 계좌 개설 요청 상세 조회 -->
  <select id="findById" parameterType="int" resultType="com.boot.sound.exchange.ExchangeAccountRequestDTO">
    SELECT 
      REQUEST_ID,
      CUSTOMER_ID,
      WITHDRAW_ACCOUNT_NUMBER,
      CURRENCY_TYPE,
      EXCHANGE_ACCOUNT_PWD,
      STATUS,
      REQUEST_DATE
    FROM exchange_account_request
    WHERE REQUEST_ID = #{REQUEST_ID}
  </select>

  <insert id="approveAccount" >

  </insert>
</mapper>
